{
  "name": "LazyVim",
  "id": "lazyvim",
  "version": "0.0.3",
  "description": "Provide essentials features for lazyvim inside devcontainers",
  "options": {
    "git": {
      "type": "boolean",
      "description": "If true, git will be installed",
      "default": true
    },
    "build_essential": {
      "type": "boolean",
      "description": "If true, build essential will be installed",
      "default": true
    },
    "wget": {
      "type": "boolean",
      "description": "If true, wget will be installed",
      "default": true
    },
    "curl": {
      "type": "boolean",
      "description": "If true, curl will be installed",
      "default": true
    },
    "python3": {
      "type": "boolean",
      "description": "If true, python3 will be installed",
      "default": true
    },
    "pip3": {
      "type": "boolean",
      "description": "If true, pip3 will be installed",
      "default": true
    },
    "python_is_python3": {
      "type": "boolean",
      "description": "If true, python-is-python3 will be installed",
      "default": true
    },
    "fd": {
      "type": "boolean",
      "description": "If true, fd will be installed",
      "default": true
    },
    "ripgrep": {
      "type": "boolean",
      "description": "If true, ripgrep will be installed",
      "default": true
    },
    "unzip": {
      "type": "boolean",
      "description": "If true, unzip will be installed",
      "default": true
    },
    "lazygit": {
      "type": "boolean",
      "description": "If true, lazygit will be installed",
      "default": true
    },
    "install_nvim": {
      "type": "boolean",
      "description": "If true, neovim will be installed",
      "default": true
    },
    "nvim_version": {
      "type": "string",
      "proposals": ["v0.9.4", "v0.9.5", "stable", "nightly"],
      "default": "stable",
      "description": "Select the neovim version you want to install as present in GitHub Releases"
    },
    "lazyvim_starter": {
      "type": "boolean",
      "description": "If true, lazyvim starter will be installed",
      "default": false
    }
  }
}
